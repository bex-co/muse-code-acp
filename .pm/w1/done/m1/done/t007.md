---
id: w1/m1/t007
title: Test coverage for shipped behavior
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m1/t006]
---

## Objective

Add meaningful tests for the behavior m1 shipped, asserting real behavior and failure modes — no tautological or snapshot-everything tests.

## Context

- Priority gaps to cover beyond t004/t005's happy paths: malformed JSONL lines mid-stream, child exiting 2 (usage error) and 1 (failure) without a terminal event, SIGTERM (143) vs SIGINT (130), prompt on unknown session id, double-cancel.
- Harness style: fake event generator in → assert emitted ACP notifications out (pattern from `claude-agent-acp/src/tests/helpers.ts`).

## Steps

1. Enumerate shipped behaviors vs existing assertions; list the gaps.
2. Write the missing failure-mode tests using fixtures + the echo provider.
3. Verify each new test fails when its guarded behavior is broken (mutate briefly, revert).

## Files

- `muse-code-acp/src/tests/*.test.ts`

## Acceptance criteria

- [ ] Every stop-reason mapping and error path in t004/t005 has at least one test
- [ ] Suite runs offline (echo provider / fixtures only) and passes in CI

## Out of scope

- Coverage for m2+ features
