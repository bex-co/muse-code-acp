---
id: w1/m2/t008
title: Test coverage for shipped behavior
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m2/t007]
---

## Objective

Close m2's coverage gaps with tests that assert real behavior and failure modes across the tool-call, mode, and continuity surfaces.

## Context

- Priority gaps: interleaved tool calls (parallel tool use — muse supports `--parallel-tool-calls`), tool_call updates arriving after cancel, unknown tool names, `policy_decision` values other than `allow:policy` (deny path — may require a doctored fixture), settings.json hot-changes between prompts.
- One gated integration test (`RUN_INTEGRATION_TESTS=1`, real provider) covering the m2 DoD scenario end to end; excluded from default CI.

## Steps

1. Gap list vs shipped behavior; write the missing fixture tests.
2. Add the gated real-provider integration test with token/cost guardrails (`--max-model-steps`, low effort).
3. Break-then-fix verification for each new test.

## Files

- `muse-code-acp/src/tests/*.test.ts`, fixtures

## Acceptance criteria

- [ ] Parallel/interleaved tool-call fixture renders correctly ordered ACP updates
- [ ] Default CI remains offline and green; integration test passes when explicitly enabled
- [ ] Deny/failed policy path has at least one test (doctored fixture acceptable, marked as such)

## Out of scope

- m3 feature coverage
