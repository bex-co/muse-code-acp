---
id: w1/m1/t001
title: Scaffold repo: package, tooling, CI transplant
worker: worker1
status: todo
estimate: 45m
depends_on: []
---

## Objective

Create the `muse-code-acp` project skeleton: npm package named `muse-code-acp` (bin `muse-code-acp`), TypeScript ESM targeting Node >= 22, with build/lint/format/test tooling and CI transplanted from `claude-agent-acp`.

## Context

- Template: `/Volumes/nvme4tbfish/projects/claude-agent-acp` — copy `tsconfig.json`, `eslint` + `prettier` configs, `vitest.config.ts`, and the CI workflows (`format:check`, `lint`, `build`, `test:run`, `conventional-prs.yml`, release-please).
- Name decision: `muse-code-acp` (npm name verified free). License Apache-2.0 with a NOTICE crediting Zed Industries for transplanted code.
- Dependencies: `@agentclientprotocol/sdk` (^1.3.0), `zod`. No `@anthropic-ai/*` packages.

## Steps

1. `npm init` the package: name, bin → `dist/index.js`, `main` → `dist/lib.js`, `type: module`, engines `node >= 22`.
2. Copy and trim tooling configs + npm scripts (`build`, `test:run`, `check`, `lint:fix`, `format`) from the template repo.
3. Copy CI workflows; adjust package name references; keep release-please + conventional-PR title enforcement.
4. Add LICENSE (Apache-2.0), NOTICE (Zed Industries attribution), stub README stating unofficial status.
5. Commit an empty `src/` + placeholder test so all four CI jobs run green.

## Files

- `muse-code-acp/package.json`, `tsconfig.json`, `eslint.config.*`, `.prettierrc*`, `vitest.config.ts`
- `muse-code-acp/.github/workflows/*`
- `muse-code-acp/LICENSE`, `NOTICE`, `README.md`

## Acceptance criteria

- [ ] `npm run build && npm run check && npm run test:run` all exit 0 on the fresh scaffold
- [ ] `npm pack --dry-run` shows only `dist/`, README, LICENSE, NOTICE
- [ ] No `@anthropic-ai/*` dependency anywhere in the lockfile

## Out of scope

- Any `src/` logic beyond placeholders (t002–t005)
- npm publish / release run (m3)
