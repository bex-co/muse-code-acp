---
id: w1/m2/t009
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m2/t008]
---

## Objective

Close milestone m2 once its definition of done actually holds (real-provider turn with tool calls/diffs renders correctly, modes and continuity verified) — not merely when the code is written.

## Context

- Standing closeout procedure: task statuses → done, move `tNNN.md` to `w1/m2/done/`, mark rows `— **DONE**` + `**Status:** done` in `w1/m2/README.md`, move `w1/m2/` to `w1/done/m2/`, check the box in `w1/README.md`.

## Steps

1. Re-verify the m2 DoD: run the gated integration test and a manual real-provider smoke turn in an ACP client/harness.
2. Execute the closeout moves and status updates in all three places.

## Files

- `muse-code-acp/.pm/w1/m2/**`, `muse-code-acp/.pm/w1/README.md`

## Acceptance criteria

- [ ] DoD re-verified on the day of closing
- [ ] Board state consistent across workstream README, milestone README, and task frontmatter

## Out of scope

- Starting m3 work
