---
id: w1/m3/t006
title: README, capability matrix, Zed config, npm release
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m3/t002, w1/m3/t003, w1/m3/t004, w1/m3/t005]
---

## Objective

Ship the product surface: a README with an honest capability/degradation matrix, editor setup instructions (Zed `agent_servers`, vscode-acp), install/auth quickstart, and the first npm release cut by the release pipeline.

## Context

- Degradations to state plainly (from research): no interactive per-tool approvals (policy + LLM judge decides; report-only), no thinking stream (encrypted reasoning), no editor-fs proxying (muse edits in its own sandbox; diffs reported after the fact), mode changes apply from the next prompt, per-turn spawn latency.
- Include: minimum supported `muse` version (0.2.1) and the feature-detection stance; unofficial-adapter trademark note (Muse Code is Meta's); NOTICE attribution (Zed Industries, Apache-2.0).
- Zed example: `agent_servers: {"Muse Code": {command: "muse-code-acp"}}` — verify the exact current settings shape against Zed docs at writing time.
- Release: release-please + conventional PR titles already wired in t001 (m1); this task cuts v0.1.0 and verifies `npm install -g` on a clean machine/container.

## Steps

1. Write README: quickstart (install muse → auth → install adapter → editor config), capability matrix, degradations, troubleshooting (preamble/log locations), roadmap pointing at Meta-gated gaps.
2. Verify the editor config end to end in Zed (or vscode-acp) and screenshot/record the steps.
3. Cut the first release; verify the published tarball installs and runs `--version` + an echo smoke turn.

## Files

- `muse-code-acp/README.md`, `NOTICE`, `docs/*`
- `.github/workflows/release-please.yml` (verify only)

## Acceptance criteria

- [ ] A newcomer can go from zero to a working Muse session in their editor using only the README
- [ ] Capability matrix matches advertised ACP capabilities exactly (no over-claiming)
- [ ] `npm install -g muse-code-acp@<released>` works on a clean environment; binary passes the echo smoke turn

## Out of scope

- Submission to the ACP agent registry / Zed docs listing (follow-up inbox note after the release is public)
