---
id: w1/m3/t008
title: Test coverage for shipped behavior
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m3/t007]
---

## Objective

Close m3's coverage gaps with meaningful tests across list/load/auth/skills/MCP — real behavior and failure modes, no coverage gaming.

## Context

- Priority gaps: loading a session whose log predates the current adapter version (schema drift), export of a session with subagent lineage, auth flows with only-env vs only-stored-key, skills list on a workspace with zero project skills, MCP rejection path messaging.

## Steps

1. Gap list vs shipped behavior; write the missing tests (temp HOME/data-dir isolation throughout).
2. Break-then-fix verification for each new test.

## Files

- `muse-code-acp/src/tests/*.test.ts`, fixtures

## Acceptance criteria

- [ ] Each m3 surface has failure-mode coverage, not just happy paths
- [ ] Default CI stays offline and green; gated integration tests documented in README

## Out of scope

- Post-release follow-ups
